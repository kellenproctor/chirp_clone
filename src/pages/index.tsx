import { SignInButton, SignOutButton, UserButton, useUser } from "@clerk/nextjs";
import { type NextPage } from "next";
import Head from "next/head";

import { api } from "~/utils/api";

const Home: NextPage = () => {
  // Clerk user
  const user = useUser();
  const { data, isLoading } = api.posts.getAll.useQuery();
  // const { data, isLoading } = {data: [], isLoading: true};

  return (
    <>
      <Head>
        <title>Create T3 App</title>
        <meta name="description" content="Generated by create-t3-app" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
      <main className="flex min-h-screen flex-col items-center">
        <div className="flex w-screen justify-end px-8 py-4">
          {!user.isSignedIn ? (
            <SignInButton />
          ) : (
            <div className="flex gap-4 rounded-md border-b-4 border-b-slate-400 p-3">
              <UserButton />
              <SignOutButton />
            </div>
          )}
        </div>
        <div className="flex min-h-screen flex-col justify-center">
          {isLoading && <div className="animate-ping m-auto h-1/2 w-full text-center">🥳🥳🥳</div>}
          {data?.map((post) => (
            <div key={post.id} className="rounded-md border-4 bg-slate-400 p-4 text-center text-3xl mb-2">
              {post.content}
            </div>
          ))}
        </div>
      </main>
    </>
  );
};

export default Home;
